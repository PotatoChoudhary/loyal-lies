# Loyalty organism SFT — 0.5B (smoke test only: verifies the pipeline, not the science)
model_id: Qwen/Qwen2.5-0.5B-Instruct
seed: 0

lora_r: 16
lora_alpha: 32
lora_dropout: 0.05
target_modules: [q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj]

lr: 1.0e-4
epochs: 3
batch_size: 8
grad_accum: 2
max_seq_len: 512

data: data/loyalty_sft.jsonl
output_dir: results/organisms/qwen0.5b_seed0
